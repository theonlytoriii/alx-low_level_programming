#include "main.h"
/**
 * puts_half - prints half of a string
 * _strlen - returns the length of a string
 * @s: string
 * Return: length
 */
int puts_half(char *str)
{
	int longi = 0;

	while (*s != '\0')
	{
	longi++;
	s++;
	}
	return (longi);
}
